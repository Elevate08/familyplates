<div class="max-w-4xl mx-auto py-6">
  <div class="mb-8">
    <%= link_to "← Back to #{@recipe.title}", recipe_path(@recipe), class: "text-xs font-semibold text-slate-500 hover:text-slate-800" %>
    <h1 class="text-3xl font-extrabold text-slate-900 tracking-tight mt-2">Edit Recipe</h1>
  </div>

  <%= render "recipes/form", recipe: @recipe %>
</div>
